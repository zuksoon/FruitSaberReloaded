using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*using Valve.VR;

public class Haptics : MonoBehaviour
{
    public SteamVR_Action_Vibration hAction;

    public void Pulse(float duration, float freq, float amp, SteamVR_Input_Sources src) {
        hAction.Execute(0, duration, freq, amp, src);
    }

    public void Pulse(float duration, float freq, float amp) {
        hAction.Execute(0, duration, freq, amp, SteamVR_Input_Sources.LeftHand);
        hAction.Execute(0, duration, freq, amp, SteamVR_Input_Sources.RightHand);
    }
}
*/
